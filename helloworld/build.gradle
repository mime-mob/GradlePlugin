apply plugin: 'java'
apply plugin: 'maven'

uploadArchives{
    repositories{
        flatDir{
            name "localRepository"
            dir "localRepository/libs"
        }
    }
}

group = "com.mime.houyi"
version = "1.0"

dependencies {
    compile gradleApi()
}
